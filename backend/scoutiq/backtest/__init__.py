"""Strategy backtesting (docs/10): apply a roster-building strategy to historical NBA
seasons and measure realized value-for-dollar vs benchmarks. Selection uses the model's
value signal as-of the decision season; grading uses REAL production (Win Shares) so the
model never grades itself."""
